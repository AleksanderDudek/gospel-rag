# In local dev: both point to localhost:8000
# In Docker Compose: BACKEND_INTERNAL_URL points to the backend service name
BACKEND_INTERNAL_URL=http://localhost:8000

# Shown in the browser tab and header
NEXT_PUBLIC_APP_NAME="Gospel RAG"
